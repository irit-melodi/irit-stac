#!/bin/bash
#SBATCH --output=irit-stac-evaluate-%j.out
#SBATCH --cpus-per-task=64
STAC=$HOME/Stac
cd "$STAC"
set -e
source "$STAC/code/cluster/env"
irit-stac evaluate "$@"
echo "DONE! irit-stac evaluate $@"
